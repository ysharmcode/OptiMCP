{
  "//": "Add OptiMCP as an MCP server. Works in Claude Desktop, Cursor, and any MCP client.",
  "//install": "pip install optimcp  (the `optimcp` command launches the stdio server)",
  "mcpServers": {
    "optimcp": {
      "command": "optimcp",
      "args": [],
      "env": {
        "OPTIMCP_HOME": "~/.optimcp",
        "OPTIMCP_DAEMON_URL": "http://127.0.0.1:8787",
        "OPTIMCP_DAEMON_TOKEN": "replace-with-your-daemon-token"
      }
    }
  }
}
